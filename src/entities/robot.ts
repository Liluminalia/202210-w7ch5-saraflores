export type ProtoRobot = {
    name?: string;
    img?: string;
    velocity?: string;
    force?: string;
    creation?: string;
    isExtra?: boolean;
};

export type Robot = {
    id: string;
    name: string;
    img: string;
    velocity: string;
    force: string;
    creation: string;
    isExtra: boolean;
};
